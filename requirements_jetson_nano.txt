# Optimized requirements for NVIDIA Jetson Nano
# Install these packages in order for best compatibility

# System packages (install via apt first - recommended)
# sudo apt install python3-numpy python3-pandas python3-matplotlib python3-scipy python3-sklearn -y

# Core data processing - use system packages when possible
numpy>=1.19.5
pandas>=1.3.0
scikit-learn>=0.24.0
matplotlib>=3.3.0
joblib>=1.0.0

# Web dashboard - install via pip
dash>=2.10.0
dash-bootstrap-components>=1.2.0
plotly>=5.10.0
flask>=2.0.0

# Serial communication
pyserial>=3.5

# Scheduling and utilities
schedule>=1.1.0
psutil>=5.8.0
tqdm>=4.60.0

# Environment variables
python-dotenv>=0.19.0

# Optional: TensorFlow for Jetson Nano
# Install using NVIDIA's pre-built wheels:
# pip3 install --pre --extra-index-url https://developer.download.nvidia.com/compute/redist/jp/v461 tensorflow

# Alternative: TensorFlow Lite (lighter weight)
# tflite-runtime>=2.5.0

# Jetson Nano GPIO (if needed for hardware control)
# Jetson.GPIO>=2.0.21

# Memory optimization tools
# memory-profiler>=0.60.0
